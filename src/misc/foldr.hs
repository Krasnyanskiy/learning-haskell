
foldr (-) 5 [1,2,3]

{-

    (1 - (2 - (3 - 5)))
    5 - initial value

    Последовательность редукции:

    foldr f ini 1:2:3:[]
    ~> 1 `f` foldr f ini (2:3:[])
    ~> 1 `f` (2 `f` foldr f ini (3:[]))
    ~> 1 `f` (2 `f` (3 `f` foldr f ini []))
    ~> 1 `f` (2 `f` (3 `f` ini))

    * - редукция редекса `f` игнорируется для примера

-}
